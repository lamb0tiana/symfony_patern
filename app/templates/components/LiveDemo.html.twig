<div {{ attributes }}>
    <!-- component HTML -->
    <p>
        {{ this.data }} {{ number }}
    </p>

    <button data-action="live#$render">Render button</button>
    <button data-action="live#action" data-live-action-param="getData">Render action</button>
</div>
