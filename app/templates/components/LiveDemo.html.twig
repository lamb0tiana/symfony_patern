<div {{ attributes }}>
    <!-- component HTML -->
    <p>
        {{ this.data }} {{ number }}
    </p>

    <button data-action="live#$render">Apply</button>
    <button data-action="live#action" data-live-action-param="getData">ll</button>
</div>
